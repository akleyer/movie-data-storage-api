[loggers]
keys=root

[handlers]
keys=console

[formatters]
keys=simple

[logger_root]
level=DEBUG
handlers=console

[handler_console]
class=logging.StreamHandler
level=DEBUG
formatter=simple

[formatter_simple]
format=%(asctime)s - %(levelname)s - %(message)s
